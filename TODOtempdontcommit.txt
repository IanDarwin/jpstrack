Map Page:

	Pause/Resume etc - enable/disable

	"Center" button to move map & display

Export - 

	List view works, add actions.

location_service
	in service/location_service.dart:
	create ensureGpsPerms() based on https://pub.dev/packages/geolocator
	call it from main: await ensureGpsPerms()

All three Note functions - if track != null, save with trackId in filename?
	or just disable if no track
